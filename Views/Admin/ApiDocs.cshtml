@{
    ViewData["Title"] = "Tài liệu hệ thống API (Swagger)";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h4 class="mb-1" style="font-weight: 800; color: var(--admin-secondary);">
            <i class="fas fa-code-branch me-2"></i> Tài Liệu API (Swagger)
        </h4>
        <p class="text-muted mb-0" style="font-size: 0.9rem;">
            Tài liệu tự động hóa dành riêng cho chấm điểm. <span class="badge bg-danger rounded-pill ms-2">Development Only</span>
        </p>
    </div>
</div>

<div class="card shadow-sm border-0" style="border-radius: 12px; overflow: hidden; height: 80vh;">
    <!-- Nhúng trang Swagger bằng Iframe để không bị vỡ giao diện Admin -->
    <iframe src="/swagger/index.html" 
            style="width: 100%; height: 100%; border: none;"
            title="Swagger API Documentation">
    </iframe>
</div>
